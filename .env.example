# Environment Configuration for Appwrite Deployment

# Appwrite Function URL (update after deployment)
APPWRITE_FUNCTION_URL=https://cloud.appwrite.io/v1/functions/YOUR_FUNCTION_ID/executions

# Optional: Rate limiting settings (in seconds)
SCRAPER_MIN_DELAY=1
SCRAPER_MAX_DELAY=3

# Optional: HTTP timeout (in seconds)
HTTP_TIMEOUT=30
